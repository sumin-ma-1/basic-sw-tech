# Optional defaults (Streamlit sidebar can override Ollama URL)
OLLAMA_BASE_URL=http://127.0.0.1:11434

# This app defaults to 8502 so it does not clash with another app on 8501.
# excel-ai-chat reads this; plain `streamlit run` uses .streamlit/config.toml.
STREAMLIT_SERVER_PORT=8502
